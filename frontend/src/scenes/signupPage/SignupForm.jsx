import { Formik } from 'formik';

const SignupForm = () => {

    return (
        <Formik>
            {() => (
                <form>
                    this is a form
                </form>
            )}
        </Formik>
    )

};

export default SignupForm; 
