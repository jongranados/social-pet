import { Formik } from 'formik';

const Form = () => {

    return (
        <Formik>
            {() => (
                <form>
                    this is a form
                </form>
            )}
        </Formik>
    )

};

export default Form; 
